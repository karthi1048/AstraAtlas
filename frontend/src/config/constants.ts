/**
 * Application-wide constants.
 * These values define AstraAtlas itself and are not environment-specific.
 */

// Default number of items displayed per page.
export const DEFAULT_PAGE_SIZE = 25;

// Default application locale.
export const DEFAULT_LOCALE = "en";

/**
 * Default timezone used for formatting.
 * UTC is used because AstraAtlas aggregates global space data.
 */
export const DEFAULT_TIMEZONE = "UTC";

// Default date locale.
export const DEFAULT_DATE_LOCALE = "en-US";
